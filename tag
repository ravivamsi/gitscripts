// Tag a commit with a message on the current branch
$ git tag <tagname> <sha1commit> -m "Custom Message"

// Tag a commit with a message on the specific branch
$ git tag <tagname> <>

// Push the tags to the origin
$ git push origin --tags

// Push the tags to the origin to a specific branch
$ git push --tags origin <brnachname>


